language: php
php:
  - 7.2.5
cache:
  directories:
    - node_modules
    - vendor
before_script:
  - cp .env.example .env
  - composer self-update
  - composer install --prefer-source --no-interaction --dev
  - php artisan key:generate